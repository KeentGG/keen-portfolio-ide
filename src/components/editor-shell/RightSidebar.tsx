export function RightSidebar() {
  return (
    <div className="flex flex-col h-full shrink-0 w-[300px] bg-ide-bg-deep rounded-ide-panel shadow-[var(--ide-shadow-panel)]">
      {/* Empty right panel — terminal / extensions / info area */}
    </div>
  )
}
